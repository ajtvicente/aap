---
- name: version
  hosts: localhost
  tasks:
    - name: view hostname
      ansible.builtin.command: hostname
    - name: view uptime
      ansible.builtin.command: uptime
    - name: view user
      ansible.builtin.command: whoami
